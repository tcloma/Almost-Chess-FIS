import React from "react"

const Bishop = ({xpos, ypos}) => {

   const bishopMoves = () => {
      // onClick it will highlight the box of the piece then show the possible movements
      // Note: The bishop moves in an "x" pattern
      // if bishop is on black tile, 
      // then it can move in that direction. 
      // max movement = withing 8x8 board

   }
   return (
      <p> Bishop </p>
   )
}

export default Bishop;